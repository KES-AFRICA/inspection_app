# Rules Project

1. generes mes commits en francais avec la presyntaxe que j'utilise. [CREATE], [UPD], [DLT]
2. Ne jamais exécuter les commits git automatiquement. Quand le commit est demandé, fournir uniquement le texte du message de commit au format markdown sans exécuter de commande `git commit`.
